package code;

public class MatrixNode extends Node {

    public MatrixNode(String state, Node parentNode, String operator, int depth, int pathCost) {
        super(state, parentNode, operator, depth, pathCost);
    }
}
